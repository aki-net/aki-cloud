# Backend configuration
BACKEND_PORT=8080
FRONTEND_PORT=3000
FRONTEND_API_BASE=http://YOUR_IP:8080

# Node identification
# NODE_ID is auto-generated deterministically from CLUSTER_SECRET + NODE_NAME
# No need to specify it in .env
NODE_NAME=node-1

# DNS configuration (set during installation, not editable from UI)
NS_LABEL=dns
NS_BASE_DOMAIN=aki.cloud

# Secrets (must be consistent across cluster)
JWT_SECRET=your-jwt-secret-here
CLUSTER_SECRET_FILE=/data/cluster/secret

# Performance tuning
SYNC_INTERVAL_SECONDS=15
RELOAD_DEBOUNCE_MS=1500

# Backend protection (optional overrides)
# API_RATE_LIMIT_PER_MIN=300
# API_RATE_LIMIT_BURST=450
# LOGIN_RATE_LIMIT_PER_MIN=10
# LOGIN_RATE_LIMIT_BURST=20
# LOGIN_LOCK_TIERS=5:30s,10:5m,20:30m,50:2h,100:24h
# LOGIN_FAILURE_RESET_SECONDS=86400
# API_MAX_BODY_BYTES=1048576
# API_MAX_HEADER_BYTES=16384
# API_IDLE_TIMEOUT_SECONDS=60

# Edge protection (optional overrides)
# EDGE_LIMIT_CONN_PER_IP=200
# EDGE_LIMIT_REQ_PER_IP=120
# EDGE_LIMIT_REQ_BURST=240
# EDGE_LIMIT_REQ_PER_HOST=8000
# EDGE_LIMIT_REQ_HOST_BURST=16000
# EDGE_LIMIT_REQ_NODELAY=false
# EDGE_CLIENT_MAX_BODY=64m

# CoreDNS protection (optional overrides)
# COREDNS_CACHE_SUCCESS_TTL=900
# COREDNS_CACHE_DENIAL_TTL=60
# COREDNS_EDNS_BUFFER=1232
# COREDNS_QUERY_LOG=false
# COREDNS_QUERY_LOG_CLASS=denial
